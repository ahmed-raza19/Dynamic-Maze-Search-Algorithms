# Dynamic-Maze-Search-Algorithms
A Python-based AI project that implements and compares seven different pathfinding algorithms (BFS, DFS, UCS, DLS, IDS, Greedy Best-First, and A*) in a dynamically changing maze environment.
